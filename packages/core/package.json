{
  "name": "@outfox/core",
  "version": "0.3.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "dev": "tsc --watch",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@vitest/coverage-istanbul": "4.1.2",
    "vitest": "^4.1.2"
  }
}
